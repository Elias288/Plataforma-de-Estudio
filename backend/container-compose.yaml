# DEV
version: '3.9'

services:
  db:
    image: docker.io/postgres:latest
    container_name: database
    restart: unless-stopped
    shm_size: 128mb
    env_file: .env
    ports:
      - 5432:5432
    # networks:
    #  - pataformaPuntoCom-network

networks:
  pataformaPuntoCom-network:
    driver: bridge
